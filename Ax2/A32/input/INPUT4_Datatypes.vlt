// Velvet - Example 4: the program is lexically correct about the SOFIA city 

_main() {
	ent #foundationYear = -7000;
	decimal #area = 492.0;
	decimal #HDI = 0.871;
	chain &countryName = "Bulgary";
	_print(&countryName);
}
